import React from "react";
import { Box, Container, IconButton, Typography, Button } from "@mui/material";

const EditProfile = () => {
  return <Container maxWidth="lg">editProfile</Container>;
};
export default EditProfile;
